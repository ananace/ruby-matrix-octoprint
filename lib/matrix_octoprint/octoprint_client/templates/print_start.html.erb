<%-
if job.key?('filament') && job.filament
  print_info = "(%s / %.2fg PLA / %s)" % [job.filament.first.last.length.as_length, job.filament.first.last.volume * 1.24, job.file.size.as_size]
else
  print_info = "(%s)" % job.file.size.as_size
end
-%>
<%- if event.user || event.owner -%>
<%= event.user || event.owner %> started printing <%= event.name %> <%= print_info %>
<%- else -%>
Started printing <%= event.name %> <%= print_info %>
<%- end -%>
<%- if job.estimatedPrintTime %>
ETA: <%= job.estimatedPrintTime.as_duration %>
<%- end -%>
